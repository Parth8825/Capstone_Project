<%- include('../layouts/header'); -%>
    <link href="/css/inventory/inventory.css" rel="stylesheet">
    </head>

    <body>
        <header>
            <h2>Employee Management System</h2>
        </header>
        <%- include('../layouts/navigation'); -%>
            <section>
                <main>
                    <% if(typeof(message) !='undefined' ){ %>
                        <div class="messages">
                            <% if(locals.message){ %>
                                <%= message%>
                                    <% }%>
                                        <% } %>
                        </div>
                        <div class="btn">
                            <a href="/schedule/addSchedule">
                                <button class="btn">Create Schedule</button>
                            </a>
                        </div>
                        <h1 id="itemDetails">Schedule</h1>
                        <table class="content-table">
                            <thead>
                                <tr>
                                    <th>Employee Name</th>
                                    <th>Day</th>
                                    <th>Start Time</th>
                                    <th>End Time</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for(var schedule of schedules){ %>
                                    <tr>
                                        <td>
                                            <%= schedule.employeeName %>
                                        </td>
                                        <td>
                                            <%= schedule.day %>
                                        </td>
                                        <td>
                                            <%= schedule.startTime %>
                                        </td>
                                        <td>
                                            <%= schedule.endTime %>
                                        </td>
                                        <td>
                                            <div class="buttons">
                                                <a class="editButton" href="#">Change</a>
                                                <a class="deleteButton"
                                                    href="/schedule/delete/<%= schedule._id %>">Delete</a>
                                            </div>
                                        </td>
                                    </tr>
                                    <% } %>
                            </tbody>
                        </table>

                </main>
            </section>
            <%- include('../layouts/footer'); -%>